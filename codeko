-- Tham chiếu đến các đối tượng GUI
local screenGui = script.Parent
local menuFrame = screenGui:WaitForChild("Frame")
local startButton = menuFrame:WaitForChild("StartButton")
local settingsButton = menuFrame:WaitForChild("SettingsButton")
local helpButton = menuFrame:WaitForChild("HelpButton")

-- Hàm xử lý nút Start
local function onStartButtonClicked()
    print("Start button clicked!")
    -- Thực hiện hành động bắt đầu trò chơi
end

-- Hàm xử lý nút Settings
local function onSettingsButtonClicked()
    print("Settings button clicked!")
    -- Thực hiện hành động mở cài đặt
end

-- Hàm xử lý nút Help
local function onHelpButtonClicked()
    print("Help button clicked!")
    -- Thực hiện hành động mở trợ giúp
end

-- Kết nối các hàm với các sự kiện nhấp chuột
startButton.MouseButton1Click:Connect(onStartButtonClicked)
settingsButton.MouseButton1Click:Connect(onSettingsButtonClicked)
helpButton.MouseButton1Click:Connect(onHelpButtonClicked)
